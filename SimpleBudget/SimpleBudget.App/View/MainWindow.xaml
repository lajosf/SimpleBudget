<Controls:MetroWindow x:Class="SimpleBudget.App.View.MainWindow"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
         Title="SimpleBudget"
         Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid Name="dataGrid" Grid.Row="0" AlternatingRowBackground="Brown" AlternationCount="2" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Group" Binding="{Binding Group}" />
                <DataGridTextColumn Header="Income" Binding="{Binding Type}" />
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" />
                <DataGridCheckBoxColumn Header="Periodic" Binding="{Binding Periodic}" />
                <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="1" Name="SaveIncomeButton"  Content="Save" HorizontalAlignment="Left" Click="SaveIncomeButton_Click"/>
    </Grid>
</Controls:MetroWindow>
